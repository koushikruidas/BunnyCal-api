global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'bunnycal-app'
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ['bunnycal-api:8080']
        labels:
          service: 'BunnyCal'
